# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'VinylizR' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VinylizR
  pod 'Realm'
  pod 'Alamofire'
  pod 'Material'
  
  target 'VinylizRTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VinylizRUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
